不在maven中央库的jar包可以放在这儿。

然后自行编辑install-maven-lib.bat文件，按照格式添加。

注意“install-maven-lib.bat”文件以来java和maven环境。请自行设置。

环境设置：
1、设置“JAVA_HOME”环境变量，内容为jdk的根目录。
2、设置将maven下bin目录添加到path中。
3、如果能在cmd中指向mvn命令则表示设置成功。